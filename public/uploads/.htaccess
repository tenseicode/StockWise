# Block direct PHP execution inside uploads.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule \.php$ - [F,L]
</IfModule>
<FilesMatch "\.(php|php[0-9]|phtml)$">
    Require all denied
</FilesMatch>
